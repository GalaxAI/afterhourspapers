[project]
name = "afterhours_papers"
version = "0.1.0"
description = "Scientific paper implementations as well as other ML concepts"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "tinygrad>=0.12.0",
]
